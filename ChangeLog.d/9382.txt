API changes
   * Align the mbedtls_nist_kw_wrap() and mbedtls_nist_kw_unwrap() functions
     with the PSA Crypto API. Instead of taking a mbedtls_nist_kw_context as an
     argument, this function now takes a mbedtls_svc_key_id_t key type.
